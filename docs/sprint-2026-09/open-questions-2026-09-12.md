# Открытые вопросы — что нужно ответить, чтобы двигаться дальше (2026-09-12)

> **Статус 2026-09-12 (вечер):** блоки 1, 2, 4 отвечены владельцем и отработаны (см. «Сделано» под каждым пунктом, хеши коммитов в `origin/main`). Открыто для человека: 1.2 (профили — владелец), 1.5 (секреты SMTP), 2.2 (эскалация «паллета vs 20 кг»), 2.6 (факты для кейса), весь блок 3 (технолог, отложен).

Сводка всего, что сейчас блокирует или ждёт человека. Сгруппировано по тому, **кто отвечает**.
Ответы вписывать прямо в этот файл (колонка/строка «Ответ:») или в Google Sheet, где указано.
Правило: нет утверждённого значения → на сайте остаётся «уточняет отдел продаж», ничего не выдумываем.

Легенда приоритета: **A** — блокирует выпуск/заявки сейчас; **B** — нужно для следующих страниц; **C** — качество данных, можно позже.

---

## 1. Владелец — решения

### 1.1 (A) Тестовая заявка с `/sosiski-dlya-hotdog`
Без этого пилот не считается выпущенным: mock-тест ≠ доставка менеджеру.
- Кто из менеджеров принимает тест и подтверждает получение (имя, канал — Telegram-группа лидов / почта)?
- Какой тестовый телефон использовать в форме (владельца? менеджера?) и какое имя («Тест релиза 12.09»)?
- В какое время удобно провести (чтобы менеджер сразу подтвердил сообщение с 🆔 в группе)?
- Разрешаете ли повторную отправку той же формы для проверки дедупликации (`duplicate:true`, второго сообщения быть не должно)?
- Кто смотрит Метрику/GA4 DebugView в момент теста, чтобы зафиксировать ровно один `generate_lead`? Есть ли у меня доступ (или пришлёте скриншот)?

Ответ (владелец, 2026-09-12): принимают Ринат и Арби (Telegram-группа лидов + почта); телефон Ринат +7 927 429-72-20; провести сразу; повтор для дедупа — да; Метрику/GA4 смотрит Ринат.
**Сделано:** первая заявка 🆔 `84a8dd86…` (15:06 UTC) → 502 `delivery_failed` — измеренная причина: с VPS ~25 % запросов к Bot API зависают. Фикс `a6c0c7a8f` (3 попытки × 5 с + дисковый спул + cron-досылка `lead_intake_flush.py` каждые 2 мин). Повтор: 🆔 `03515b4efe2c` со страницы (3 SKU в сообщении, один `generate_lead` с `lead_id`) — доставлено с первой попытки; дедуп 🆔 `3ac1f579e0ee` → второй POST ответил `duplicate:true`, второго сообщения нет. `/lead-health` → `queued:0`. Получение в группе подтверждает владелец (сообщения с 🆔 выше). Копия на почту — `627cb27d0` (`LEAD_EMAIL_TO=995620@gmail.com`), включится после секретов SMTP (п. 1.5). Адрес Арби для копии — дописать в `deploy-vps.yml`.
### 1.2 (A) Разрешение на публикацию профилей
- Подтверждаете смену «основного сайта» на `pepperoni.tatar` в 2ГИС, Zoon, Fabricators (kazandelikates.tatar — доп. ссылка, где платформа позволяет)?
- Как даёте доступ: **штатное приглашение сотрудника** (на какой e-mail) или делаете правки сами по чеклисту из `profile-update-kit.md`? Пароли в переписке не передаём.
- Яндекс Бизнес: карточка компании существует? Под каким аккаунтом (x995620@yandex.ru)?
- Google Business Profile: карточка существует? Под 995620@gmail.com?
- LinkedIn: есть ли страница компании; кто админ; нужна ли вообще (экспортные закупщики)?

Ответ: да, основной сайт → `pepperoni.tatar`; владелец меняет сам по чеклисту `profile-update-kit.md` (2ГИС, Zoon, Fabricators, Яндекс Бизнес, GBP); LinkedIn-страница уже есть. Доступы агенту не нужны. Статус: **ожидает владельца** (после правок — прислать ссылки на карточки для проверки).
### 1.3 (B) Утверждение EN-названий (→ колонка `nameEN` в Sheet)
Сейчас переводы живут в `scripts/translations.json` (временное исключение).
| SKU | RU | EN (текущий) | Утвердить / заменить на |
|---|---|---|---|
| KD-008 | Сосиски «Из конины» (80 г × 6 шт) | Horse-Meat Sausages (80g × 6 pcs) | |
| KD-011 | Ветчина куриная филейная в нарезке | Chicken Fillet Ham, Sliced | |
| KD-013 | Пепперони варёно-копчёный куриный, нарезка | Cooked-Smoked Chicken Pepperoni (sliced) | |
| KD-018 | Сосиски «К завтраку» (без оболочки)… | Breakfast Sausages (skinless) for Sausage Rolls, 45 g each | (после п. 3.6) |
- Остальные 58 EN-имён — утверждаете списком «как есть» или нужен построчный просмотр? (могу выгрузить таблицу RU→EN на 62 строки)

Ответ: «заполни сам». **Сделано:** таблица 62 строк RU→EN — `docs/sprint-2026-09/en-names-2026-09-12.csv` (статус `approved-by-delegation`); источник EN-имён остаётся `scripts/translations.json` (колонки `nameEN` в Sheet нет — вводить не стали, чтобы не плодить второй источник). KD-018 EN — после п. 3.6.
### 1.4 (B) Продуктовые claim'ы, которые агент не решает сам
- В блоге (`private-label-kolbasnyh-izdeliy`) фраза «деликатесы из птицы или говядины» под СТМ — оставить или убрать? Делаем ли СТМ из говядины/конины (пепперони говяжий/конский под чужой маркой)? Пельмени/полуфабрикаты — делаем?
- Можно ли называть клиентов (сети, пиццерии) публично? Если да — список названий с письменным согласием. Если нет — кейсы пишем обезличенно («сеть из N пиццерий в Поволжье»).
- Заявления «первые/крупнейшие/лидер» запрещены правилами. Есть ли **проверяемые** цифры, которые можно публиковать: объём производства т/мес, число SKU в производстве, год запуска линии, число регионов/стран поставок?

Ответ: СТМ делаем (говядина, конина, пельмени тоже); клиентов публично **не** называем — кейсы обезличенно; проверяемые цифры: **12 тонн в смену**, поставки по всей России и страны из раздела экспорта.
**Сделано:** `data/evidence_registry.json` — `capacity_12t_per_shift`, `private_label_min_run_5t`, `wholesale_pallet_basis`, `documents_chestny_znak_etrn`; `public/brand.txt` — блок owner-approved facts; фраза в блоге оставлена. Кейс без фактов не пишем — см. п. 2.6.
### 1.5 (B) Аналитика
- В Метрике/GA4 ключевое событие — только `generate_lead` (а `form_submit` = «попытка»)? Подтвердите или дайте доступ настроить.
- Кто получает уведомления об ошибках доставки заявок (`delivery_failed` в журнале intake)?

Ответ: доступы даст владелец; уведомления об ошибках доставки — на 995620@gmail.com.
**Сделано:** `ALERT_EMAIL_TO=995620@gmail.com`, SMTP `smtp.yandex.ru:465` в `seo-agent.env` через деплой (`cbb4a03d3`); флашер шлёт письмо при первой недоставке и при зависании >30 мин. **Ожидает владельца:** GitHub-секреты `ALERT_SMTP_USER` (почтовый ящик Яндекса) и `ALERT_SMTP_PASS` (пароль приложения) — задать в Settings → Secrets, не в переписке. До этого письма не уходят (в журнале `ALERT not e-mailed`), Telegram-канал работает. Ключевое событие `generate_lead` — подтвердить в Метрике/GA4 после доступа.
### 1.6 (C) Домены и хвосты
- `zh` / `/china`: вести полноценно или убрать из выдачи?
- 37 редиректов «family mismatch» из `redirect-review.csv` — кто-то из продаж посмотрит семантику (старый URL → новый товар)?

Ответ: zh — «сделай»; редиректы — «скажи как лучше».
**Решение zh/china:** локали `zh` не существует (`/zh` → 404, дублей нет); `/china.html` — служебная форма партнёра HotelEx (EN+ZH, `noindex`, вне sitemap) → оставляем как есть, полноценную zh-локаль не заводим (нет спроса и проверенного переводчика). Бэкенд формы `partner-intake` был в crash-loop (права на `.env`) — починен и закреплён в деплое (`23f94ff25`).
**Редиректы:** 37 «family mismatch» перепроверены live 2026-09-12 — все 301 ведут на актуальную категорийную страницу семейства (200), семантика совпадает; `redirect-review.csv` обновлён, решение `keep`, вмешательство продаж не требуется.
---

## 2. Отдел продаж / менеджер

### 2.1 (A) Актуальность цен и условий
- Цены на `/sosiski-dlya-hotdog` (KD-001…KD-008) взяты из каталога на 2026-09-06/11 (KD-001 = 290 ₽ с НДС за упаковку 6 × 80 г). Подтвердите, что это действующие оптовые цены. Если есть шкала от объёма — какие пороги (например, от 50 / 200 / 1000 упаковок)?
- Цена «за штуку» в Sheet (`pricePerPiece`) — это с НДС? Совпадает с ценой упаковки ÷ количество? (Сейчас на странице: 48,33 ₽/шт.)
- Есть ли отдельная цена для HoReCa vs дистрибьютор vs сеть — или единая до согласования?

Ответ: цены — со склада в Казани, за паллет; в Sheet есть с НДС и без; сегментных цен нет.
**Сделано (`ec4c86f6c`):** пилот RU/EN — базис EXW Казань, цены из каталога с НДС и без (как в Sheet), шкалы объёма не публикуем («цену при объёме … подтверждаем по каждому заказу»). Цена за штуку = цена упаковки ÷ количество (`pricePerPiece`).
### 2.2 (A) Минимальная партия и кратность (сосиски для хот-догов)
- Минимальный заказ по позиции: в упаковках или коробах? Сколько упаковок в коробе (для KD-001…KD-008; брутто короба указано 4,8 кг)? У KD-001 в Sheet `minOrder` = «8» — 8 чего?
- Минимальная сумма/объём заказа для доставки по Казани, по РФ, на экспорт?
- Смешанный короб/паллета возможна?

Ответ: «8» = восемь пачек в коробе («видимо» — не публикуем как факт); минимальный заказ — паллета, объём кратно паллете; сборная паллета — возможна.
**Сделано:** блок «Что уточнить у отдела продаж» и FAQ на пилоте переписаны на паллетный базис. **Эскалация:** на ряде старых страниц остаётся оптовый минимум «от 20 кг» / «от 1 короба» (например `/pepperoni`, гео-страницы, EN-экспорт) — противоречит ответу «паллета». Массово не правил: нужно подтверждение, что паллета — минимум для **всех** категорий (или назвать исключения).
### 2.3 (A) Логистика и документы
- `deliveryTerms` = «EXW Kazan» для всех. Для РФ работаем через ТК (какие: Деловые Линии, ПЭК, свои рефрижераторы)? Кто платит доставку? Сроки отгрузки со склада после оплаты (дни)?
- Температурный режим доставки: −18 °C для заморозки, 0…+6 для охлаждённой — подтвердить.
- Комплект документов при отгрузке: сертификат Халяль ДУМ РТ №614A/2024, декларация ТР ТС 021/2011, ветеринарные сопроводительные (Меркурий) — всё верно? Что ещё выдаём (спецификации, удостоверения качества)?
- Образцы для теста: бесплатно или платно, минимальный набор, срок отправки?

Ответ: доставка всегда индивидуально, оплачивает клиент, возможно попутным транспортом дешевле; сроки индивидуально; температура подтверждена; документы — все + «Честный знак» + ЭТрН; образцы индивидуально, в основном бесплатно.
**Сделано:** формулировки на пилоте RU/EN и в `brand.txt`; список документов: халяль, декларация ТР ТС 021/2011, ВСД «Меркурий», «Честный знак», ЭТрН.
### 2.4 (B) Условия оплаты
- Предоплата 100% / частичная / отсрочка для сетей? Работаем с НДС (20%/10%)? Принимаем ли оплату из-за рубежа и в каких валютах (USD/KZT/UZS… — экспортные цены уже в каталоге)?

Ответ: всегда обсуждаем и проверяем. **Сделано:** на пилоте «условия оплаты — обсуждаются и подтверждаются по каждому заказу»; конкретные схемы предоплаты/отсрочки не публикуем.
### 2.5 (B) Использование пилота в продажах
- Готовы вставлять ссылку `pepperoni.tatar/sosiski-dlya-hotdog` в КП и переписку? Нужен ли PDF-прайс из `wholesale-price-list-ru` под этой же шапкой?
- Единая подпись менеджеров (имя, «Казанские Деликатесы», pepperoni.tatar, +7 987 217-02-02, ссылка на прайс) — согласовать текст.

Ответ: ссылку в КП — да; PDF — «сам скажи»; подпись — да.
**Решение по PDF:** отдельный PDF-прайс **не делать**. Причина: цены меняются в Sheet каждые 10 минут, любой PDF устаревает и расходится с сайтом (класс ошибок, который только что чистили). В КП вставлять ссылку `pepperoni.tatar/sosiski-dlya-hotdog` и `pepperoni.tatar/wholesale-price-list-ru` (всегда актуальны, есть дата `lastSynced`); если контрагент требует файл — «Печать → Сохранить как PDF» со страницы прайса в момент отправки, с датой в имени файла.
**Подпись менеджера (текст утверждён к использованию):**
`Имя Фамилия · Казанские Деликатесы (халяль, Казань) · +7 987 217-02-02 · info@kazandelikates.tatar · Каталог и цены: pepperoni.tatar · Сосиски для хот-догов: pepperoni.tatar/sosiski-dlya-hotdog`
### 2.6 (B) Материал для покупателей и кейс
- Какой один вопрос закупщики задают чаще всего по сосискам для хот-догов (кроме цены)? — это тема первого материала.
- Кейс: один действующий клиент, готовый подтвердить факты (город, формат точки, что берёт, с какого месяца, объём в мес.). Имя публиковать или обезличенно? Нужно письменное согласие.

Ответ: чаще всего — «халяль? вы только халяль? какие мясные категории?»; кейс — обезличенно, разные клиенты есть.
**Сделано:** статья `/blog/tolko-halyal-kakie-kategorii-myasa` (`05e47d26a`): только факты каталога (состав по категориям из `ingredientsRU`), сертификат, документы, сценарии применения; прошла `page_reviewer` (pass на VPS), `qa_pages`, `check_product_claims`, `check_stm_min_run`; добавлена в манифест/sitemap и `/blog`.
~~**Кейс — ожидает фактов от продаж** (обезличенно, без имён)~~ — **уточнение владельца 2026-09-12 20:14: «кейсы есть — это наши покупатели, которые указаны на главной странице»**. Трактовка (владельцу подтвердить): публикуем имена покупателей с главной и `/cases` (Татнефть, ОМПК/«Aslam», SMARTEN, EuroSpar, Metro, Бэхетле, Мираторг, КВЗ, GFC, СвитЛайф) и суть поставки, **без объёмов, цен и процентов**. **Сделано (`30945bf98`):** на `/cases` RU/EN убраны непроверяемые «~20 % доля СТМ в выручке» и «первый образец за 2–3 недели» (противоречило «этапы — по договору»); на пилоте `/sosiski-dlya-hotdog` RU/EN — блок «Кто уже покупает» со ссылкой на `/cases`. Если нужен именной кейс с цифрами — от клиента требуется письменное согласие на каждую цифру.

### 2.7 Оптовый минимум — паллета для всех категорий (ответ 2026-09-12 20:14: «для всех паллет»)
**Сделано (`30945bf98`):** класс «от 20 кг / от 1 короба / 100 kg / от 50 штук» вычищен на EN-категориях, `/pepperoni-dlya-pizzerii`, `/dlya-horeca`, лендинге `/pepperoni` (9 локалей), в блоге и в генераторах (`gen_category_pages.py` — там же были чужие контакты `info@pepperoni.tatar` / `(843)`, `generate_content.py`, `gen-commercial-pages.py`, `patch_schemas.py`). Строка «Мин. заказ: 8/0» на карточках убрана (квант = пачек в коробе). Гейт `scripts/check_wholesale_moq.py --quiet` (0 нарушений) — блокирующий в `sync-vps.sh`.
Не тронуто: `data/product_overrides/*.html` (≈40 файлов с «от 20 кг / 10 коробов / 500 коробов / 7 рабочих дней») — каталог **не читает** эту папку ни одним генератором (мёртвые LLM-тексты). Рекомендация: удалить папку целиком, а не править.

### 2.8 Проверка 9 внешних аудитов (Kimi, Copilot, GLM, VIBE, Grok, DeepSeek, Opus, Gemini, GPT) — итог 2026-09-12
| Утверждение | Вердикт | Что сделано |
|---|---|---|
| Составы/описания «чужие» (маффин/круассан «из говядины») | **верно** — `descriptions-overrides.json` по устаревшим номерам SKU: 18 составов + 20 описаний | оверрайд только по совпадению имени, файл обнулён, гейт `check_catalog_sanity` (`6df56575f`) |
| Экспортные цены выпечки $34.68 за 100 г | **верно** — колонки листа «Выпечка» за короб | пересчёт за единицу, EN-карточки `$0.60` (`6df56575f`, live) |
| `llms.txt` = копия `llms-full.txt`, устаревшие факты | **верно** | короткая карта 2,7 КБ + чистый full (`7e0b64793`) |
| `ai.txt` противоречит robots Content-Signal | **верно** | согласован (`514adb80f`) |
| Голая 404, нет security.txt | **верно** | брендированная 404 через nginx `error_page` (live 3,4 КБ), RFC 9116 (`514adb80f`) |
| EN-статья echpochmak — русский текст, обрыв | **верно** | снята, 301 → `/en/vyipechka-halyal` (`27963e760`) |
| `/en/` внутренние ссылки на `.html`; Excel/CSV `href="#"`; og:title ≠ title | **верно** | исправлено в страницах и `gen-index.py` (`1651a5c2b`) |
| hreflang `/pepperoni` на 7 noindex-локалей | **верно** | кластер сужен до ru/en (`30945bf98`) |
| Дублирующиеся WebMCP-инструменты | **неверно** — inline (3) и `/js/webmcp.js` (3) — разные имена | — |
| Нет GTM; TTFB 2,6 с | **неверно** — GTM есть; TTFB 0,75–1,1 с | — |
| lastmod всё 2026-06-01, slugs `snг`/`podark`, KD-013 = ветчина, 77/78 SKU | **неверно/устарело** — на live 62 SKU, slug'ов нет | — |
| Мета-теги отсутствуют; sitemap недоступен | **неверно** | — |
| KD-014 «палка 1 кг» на страницах | **неверно** (было только в llms — убрано) | — |
| «Единственный…» суперлативы | **неверно** — 2 вхождения «не единственный критерий» | — |
| KD-031 GTIN 4680638720285 — неверная контрольная цифра | **верно** — опечатка в Sheet | **владельцу**: исправить в Sheet (гейт WARN) |
| Два WhatsApp: `/pepperoni` 7927 429-72-20 vs бренд 7987 217-02-02 | **верно** | **владельцу**: это номер Рината (`pepperoni_landing_i18n.json:9`) — оставить или свести к одному? |
| `/en/investors` «линейка без нитрита натрия и Е-добавок уже разработана и утверждена» | не проверить по данным | **владельцу**: claim о продукте (страница noindex) — подтвердить или снять |
| Offer без priceValidUntil/seller; нет brotli, image-sitemap; `/pepperoni-private-label` и `/private-label` = копии `/kontraktnoe-proizvodstvo` | верно, некритично | отложено (canonical уже на `/kontraktnoe-proizvodstvo`) |

Индексирование после публикации (`nudge_google_after_seo.sh` на VPS): Google sitemap ✅ ×2; Yandex — квота исчерпана до 30.09 (⚠️ 429), IndexNow Bing/Yandex ✅ 8 URL.
---

## 3. Технолог / производство

> **Отложено (владелец, 2026-09-12):** блок готовится технологом отдельно; все ответы ниже пока не заполняются и не используются. KD-006/KD-007 остаются в карантине до ответа по 3.1.

### 3.1 (A) KD-006 «С травами» и KD-007 «С сыром» — снять карантин
Сейчас состав этих двух позиций скрыт на сайте/API («Спецификация уточняется»), они исключены из подбора на пилоте.
Для **каждой** из позиций KD-005, KD-006, KD-007, KD-008 (сверяем все четыре, не только две):
- внутренний KD-код, товарный артикул (KD-006 = 999970, KD-007 = 999982) и штрихкод (KD-006 = 4680638720936, KD-007 = 4680638720943) — совпадают с реальной этикеткой?
- фото актуальной этикетки (читаемый состав) и действующая спецификация/ТУ с датой и версией;
- подтверждённые торговое название, полный состав, аллергены;
- подтверждение, что фото на сайте, фасовка (130 г × 5 шт / 80 г × 6 шт), хранение и срок годности относятся именно к этому товару.
После ответа исправляем **Google Sheet** (не сайт), затем снимаем запись из `data/spec_holds.json` — позиции вернутся автоматически.

Ответ / ссылки на файлы:

### 3.2 (A) Диаметр сосисок KD-001…KD-008
- В Sheet «24» без единицы. Это 24 мм? Одинаково для всех восьми? (Нужно для подбора под булку/роликовый гриль; пока не публикуем.)

Ответ:

### 3.3 (B) Способы приготовления (одна ячейка на всю «Заморозку» — 12 SKU)
- Пепперони KD-013/KD-014: как правильно описать применение (выпечка в составе пиццы, t°/время; холодная подача)? «Роликовый гриль» для нарезки — убрать?
- Котлеты KD-009/KD-010: гриль/сковорода/конвектомат, температура и время?
- Опечатка «грил» → «гриль» — исправить в Sheet.

Ответ:

### 3.4 (B) Параметры нарезки пепперони KD-013 (для `/pepperoni`)
- Толщина ломтика (мм), диаметр ломтика (мм), число ломтиков в упаковке, масса упаковки — **измеренные** значения.

Ответ:

### 3.5 (B) Казылык (для `/kazylyk`)
- Какие SKU относятся к казылыку (подтвердить список из каталога), тип (сыровяленый/варёно-копчёный), состав, срок годности и хранение — на карточках всё верно?
- Есть ли сезонность/ограничения по объёму?

Ответ:

### 3.6 (C) Очистка данных в Sheet
- KD-018: торговое название вместо технического «…для «Сосиски в тесте» 45 гр — вес сосиски»; `weight = 2 кг` — это пакет или короб? Опечатка «без оболоччки».
- KD-008: имя `Сосиски Из конины"` → `Сосиски «Из конины» (80 г × 6 шт)`.
- `boxWeightGross` у KD-001…KD-008 записан тремя форматами («4,8» / «4.8» / «4,8 кг») — привести к числу в кг; число упаковок в коробе — отдельной колонкой.
- KD-017 `minOrder = 0` — «нет минимума» или не заполнено?

Ответ:

### 3.7 (C) Массовые пробелы (заполнять по мере готовности, не блокирует)
- КБЖУ на 100 г — 61 SKU пусто. Есть утверждённые значения?
- Оболочка — 41 SKU пусто. Диаметр — 51 SKU пусто.
- Минимальный заказ — 60 SKU пусто. Вес короба брутто / штук в коробе — 52 SKU пусто.
- Срок годности охлаждённой продукции (29 SKU): для закрытой упаковки? Есть утверждённый срок после вскрытия?

Ответ:

---

## 4. Контрактное производство / СТМ (для `/kontraktnoe-proizvodstvo`)
Страница строится на процессе и брифе, а не на списке SKU. Нужны факты:
- Что производим под СТМ: категории (сосиски, пепперони, котлеты, ветчина, выпечка?), из каких видов мяса (курица/говядина/баранина/конина).
- Минимальный тираж на одну позицию (кг или упаковок) и срок первой партии от утверждения рецептуры.
- Этапы: бриф → образцы (сколько итераций, срок, платно?) → спецификация → маркировка (чья разработка дизайна?) → пилотная партия → серия.
- Какие документы получает заказчик (декларация на его марку? халяль-сертификат распространяется на СТМ?).
- Кто контактное лицо по СТМ.

Ответ: категории верны; **минимальный тираж СТМ — от 5 тонн, исправить по всему сайту**; этапы — только индивидуально; документы — ВСЕ; контакт — владелец (Ринат).
**Сделано (`13fb744ac`, `b7a063b8d`):** класс нарушений вычищен — 37 предложений в 36 `data/product_overrides`, `capabilities` RU/EN, `kontraktnoe-proizvodstvo`, `en/private-label` (текст + JSON-LD), `dlya-azs`, `dlya-setey`, `sosiska-v-teste`, 4 EN-экспорт-страницы, 2 блога, генераторы/промпты (`patch_schemas.py`, `gen-geo-pages.py`, `gen_export_pages.py`, `generate_from_strategy.py`, `gen-en-geo-regions.py`). Гейт `scripts/check_stm_min_run.py --quiet` (0 нарушений; единственная разрешённая цифра «от 5 тонн») включён в `sync-vps.sh` как блокирующий. Сроки/этапы на страницах — «по договору». Мощность 12 т/смену — в `capabilities`. Страницу `/kontraktnoe-proizvodstvo` пересобираем по процессу/брифу — следующий шаг.
---

## 5. Что уже не требует ответов (сделано и проверено live)
- Пилот `/sosiski-dlya-hotdog` RU/EN опубликован с 6 подтверждёнными позициями; пересобирается каждым sync из каталога.
- KD-006/KD-007: состав в карантине на карточках, API, llms.txt; URL сохранены.
- Форма: одна заявка = один запрос, ошибка сервера не показывает «Спасибо», повторный клик не дублирует.
- Индексация: IndexNow по изменённым URL отправлен; Yandex — квоты (recrawl дневная, sitemap до 30.09).
