# GEO-сессия 2026-06-20 — итоги и открытые нити

## Что сделано

### Код и данные (все в `origin/main`)
| Коммит | Что |
|---|---|
| `a786751a` | GEO-формат §5: tldr-answer, use-scenario, очистка random_angle, is_valid_page |
| `d8e3fc02` | reviewer: убрать ```html fence из промпта |
| `3891af4f` | reviewer: visible text вместо raw html[:6000] |
| `97d09d3a` | data: убрать «единственный в России» из usp_ru pepperoni |
| `7cd152dcf` | bulk+reviewer: бан «Почему выбирают нас»; max_tokens 512→1024 |
| `7782fef7` | bulk: бан класса region-claims в системном промпте |
| `8f4cf9a7` | data: обогатить usp_ru реальными данными каталога (6 продуктов) |
| `f92cb903` | feat: LLM_BULK_BUDGET_USD — per-run конверт отдельно от дневного капа |

### Инфраструктура
- `LLM_DAILY_BUDGET_USD=10` — дневной кап автономного агента
- `LLM_BULK_BUDGET_USD=20` — per-run конверт bulk (в `seo-agent.env`)
- VPS на `origin/main`, SSH deploy key, `git pull --rebase` до commit в `seo-agent-vps.sh`

### Результат bulk
- Pass rate вырос: 30% → 80% → 88% по мере чистки источников
- ~499 новых страниц за сессию, ~3350 GEO-страниц всего
- Карантин: 359 страниц (правильно отклонённые — не брак, а гейт)
- Расход за месяц: ~$12.74

## Открытые нити (не горит, на следующий заход)

### 1. `sosiki-v-teste` — данные продукта
**Что:** нет веса/диаметра/длины в `usp_ru` → reviewer отклоняет по Критерию 1.
**Что нужно от владельца:** длина сосиски (мм), диаметр (мм), вес шт/упаковки, срок годности, MOQ.
**Действие:** внести в `data/products_geo.json` → ре-ревью карантина → bulk допрогон.

### 2. Карантин (359 стр.) — стратегия
**Варианты:**
- A. Оставить как есть — гейт работает, страницы не в проде
- B. После фикса `sosiki-v-teste` прогнать ре-ревью — часть пройдёт
- C. Дообогатить продуктовые данные → регенерировать

### 3. DeepSeek на генерации — решение отложено
**Контекст:** `claude_client.py` убрал DeepSeek как основную модель, но env var `DEEPSEEK_API_KEY` читается. Стоит ли держать как fallback или удалить?

### 4. Приложение №1 — операционные цифры
Методика маржи, unit economics — припарковано.

### 5. Архивация ветки `STARTUP-AIO`
Ветка отработала, можно убрать или тегнуть как архив.

### 6. VPS в pull-only режим
Сейчас VPS коммитит (seo-agent-vps.sh) — это нужно для SEO-агента.
Pull-only возможен только если генерацию перенести на CI/Cloud.

## Урок сессии
> Исправляешь источник — улучшается вся популяция разом.
> Pass с 76% до 88% не через правку страниц, а через 2 поля в `products_geo.json`.
