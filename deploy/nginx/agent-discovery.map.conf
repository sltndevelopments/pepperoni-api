# http-context map for origin Markdown negotiation (no Cloudflare proxy).
# Installed as /etc/nginx/conf.d/00-pepperoni-agent-map.conf
map $http_accept $pepperoni_wants_markdown {
    default 0;
    ~*text/markdown 1;
}
