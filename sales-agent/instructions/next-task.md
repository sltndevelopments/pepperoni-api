# next-task.md — Architect → Worker handoff (sales-agent / Стив)

## Done — 2026-07-05: дедуп-баг лидов (закрыто)

### Диагноз
`upsert_lead` (`core/store.py`) дедуплицировал только по `inn`. 547 658 из
549 756 лидов (99.6%) приходят из CRM Google Sheet без ИНН → каждый
push↔pull цикл плодил новую строку с новым `id` для той же компании.
Затронуты все 12 named targets из `named_targets.yaml` (Самокат — 53 280
дублей, +~3500/цикл).

### Фикс
- `core/store.py` (коммит `44d7ac028`): при пустом `inn` дедуп по
  `name` (COLLATE NOCASE) + `region`, с фолбэком на `name` только.
  Локально проверено (3 сценария: dedup by name+region, dedup by name
  only, разные имена не мержатся, inn-дедуп не сломан).
- `scripts/cleanup_duplicates.py` (коммит `d6cdd233b`): одноразовая
  чистка — группировка по (name, region), survivor по рангу статуса →
  богатству profile → старейший created_at, реассайн `threads.lead_id`
  / `drafts.lead_id`, бэкап `agent.db` перед удалением.

### Verify (сделано на VPS, 2026-07-05 12:00–12:10 UTC)
- Dry-run подтвердил ровно 12 групп дублей (все — named targets),
  552 034 строки на удаление.
- Реальный запуск: `leads` 552 046 без-ИНН строк → **2110 итого** после
  чистки. `Самокат`: 1 строка.
- Два повторных `crm-pull --limit 5000` подряд: 2110 → 2110 (идемпотентно,
  новых дублей не создаётся).
- Бэкап сохранён: `data/agent.bak.20260705T120712Z.db` на VPS.

## Current step

Нет открытого шага. Следующий приоритет по итогам разговора с owner —
**оценка целесообразности миграции на Claude Agent SDK** (отдельная
задача, не блокирует текущую работу воронки). Ждём решения владельца,
хотеть ли этот анализ сейчас или после того, как воронка на восстановленных
2110 лидах даст первые письма/ответы.
